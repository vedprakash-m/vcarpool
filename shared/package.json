{
  "name": "@vcarpool/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for vcarpool application",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "mkdir -p dist",
    "build": "tsc",
    "postbuild": "echo 'Build completed successfully' && ls -la dist/",
    "dev": "tsc --watch",
    "test": "echo 'Shared package: No tests to run. Build validation passed.' && exit 0",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "zod": "^3.22.4"
  }
}
